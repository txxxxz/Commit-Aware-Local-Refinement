# final-log summaries

This directory contains curated `summary.json` files extracted from the original `final-log` experiment folders. Raw logs, `rawdata/`, and `risk_trace.log` files are intentionally not copied.

See `manifest.json` for the source experiment names and skipped folders that did not contain a recoverable summary payload.
